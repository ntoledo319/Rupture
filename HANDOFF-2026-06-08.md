# EOLkits — Handoff (2026-06-08): GRACE hardening, profit levers, go-live

_Session handoff for the next implementer/operator. Supersedes the operational
state in `HANDOFF.md` (kept for launch history) and `SEO-GRACE-HANDOFF.md`._

## TL;DR — DEPLOYED & LIVE (2026-06-09)

The paid fulfillment backend was hardened, the commerce-page renderer was fixed,
profit levers were added, and **all of it is now deployed to production** on the
GRACE VPS (`vps-6e575790` / 15.204.209.97) and verified end-to-end. Everything is
committed and pushed to `origin/main`.

- Branch: `main`; Head: `38a4392`
- Prod API: `https://eolkits.com/health` → hardened build, `stripe: live`, clean startup
- Static site: `docs/` rsynced to `/var/www/eolkits`; commerce pages have 0 `{API_URL}`
- All deploy steps were scoped to `eolkits` / `eolkits-api` only — the other
  satellites on the box (marketing-bot, nonprofitradar, outreach-cockpit,
  saiditright, toledo-*, grace-backend) were verified untouched.

### What was deployed this session
1. Added the two new env vars to the VPS `.env.production` (existing live secrets
   reused): `EOLKITS_INTERNAL_URL_SECRET`, `GITHUB_APP_SLUG=eolkits-migration-bot`.
2. rsynced hardened code into `/home/ubuntu/sites/eolkits-api` (`.env.production`
   preserved), rebuilt + restarted `docker compose -p eolkits-api`. A first
   attempt crash-looped on a legacy-DB `dedupe_key` migration ordering bug; fixed
   (`store.py`) and redeployed — now healthy.
3. Added `/pack/setup` to the `eolkits.com` Caddy matcher; validated + reloaded.
4. rsynced `docs/` → `/var/www/eolkits` (snapshot first; dry-run showed no deletions).

Verified live: `/health`, `/status` (stripe live, funnel counts), `/api/events`
(200), `/api/drift/checkout` (returns a live `cs_live_` subscription session),
`/pack/install` (real install URL), `/pack/setup` (303), all commerce/drift/
success pages 200 with zero `{API_URL}`, migrate CTAs server-routed, sitemap 23.

Rollback artifacts on the VPS: `eolkits-api:rollback-2026-06-09-0451` (image),
`.env.production.bak.*`, `/tmp/eolkits-api-code-*.tgz`, `/tmp/eolkits-webroot-*.tgz`,
`/etc/caddy/Caddyfile.bak.*`.

## Commits in this work (newest first, excluding the every-5-min status bot)

- `ea7ad32` feat(growth): server-routed checkout, funnel analytics, Drift Watch, upsell + de-stale launch
- `3c5b48f` feat(grace): harden paid fulfillment + fix commerce-page render before GRACE deploy
- `6741fd3` feat(seo): deterministic deprecation-SEO renderer (pre-existing, reconciled)

## Before you redeploy (the API now fails closed)

Production is **already live**, so `/home/ubuntu/sites/eolkits-api/.env.production`
on the VPS already contains the live secrets — they are **reused** on redeploy,
not re-entered:

```
STRIPE_KEY, STRIPE_WEBHOOK_SECRET, GITHUB_APP_ID, GITHUB_APP_PRIVATE_KEY,
GITHUB_WEBHOOK_SECRET, RESEND_API_KEY, PUBLIC_SITE_URL, PUBLIC_API_URL, EOLKITS_API_PORT
```

The hardening added **two new vars** that must be appended to that same file or
startup will abort in production (it now validates the full set):

```
# Set once and keep stable; signs short-lived internal upload refs (SSRF mitigation).
EOLKITS_INTERNAL_URL_SECRET=$(openssl rand -hex 32)

# The App's URL handle; powers the /pack/install button. Confirm the real slug at
# github.com/settings/apps (repo docs say "eolkits-migration-bot" but that public
# page 404s, so verify). Non-blocking: /pack/install falls back without it.
GITHUB_APP_SLUG=eolkits-migration-bot
```

Note: these secrets are NOT reachable from the dev environment (no SSH to the
VPS; GitHub Actions secret values are write-only; the Stripe MCP can act with the
live key but never exposes its raw value). They live only on the VPS and in your
Stripe/GitHub/Resend dashboards.

## What changed

### P0 — security / revenue (apps/grace-api, apps/runner, apps/web)
- Fail-closed config + GitHub-webhook verification (no dummy Stripe defaults; no
  bypass when the secret is missing). `config.py`, `security.py`.
- Stripe webhook durability: `stripe_events` / `purchases` / retryable `jobs`
  tables, atomic idempotent event recording, durable-queue + startup drainer
  with backoff + dead-letter. `store.py`, `app.py`.
- Payment validation before fulfilling: retrieve authoritative Checkout Session;
  verify price_id (vs `pricing.yml`), amount, currency, mode, payment_status,
  livemode, metadata. Canonical Stripe Price IDs (verified live). `stripe_client.py`, `pricing.py`.
- SQLite robustness: WAL + busy_timeout + lock retry; atomic unique inserts.
- SSRF: arbitrary `upload_url` rejected; validated `upload_id` → local path /
  own-host URL; private/link-local ranges blocked. `app.py`, `audit_pdf.py`, `main.py`.
- Refund guarantee: `payment_intent`↔PR↔repo mapping; CI failure → idempotent
  Stripe refund unless `override:ci-failure`. `app.py`.
- GitHub App: real install URL + `/pack/setup` callback; installation→repo
  mapping; repo-∈-installation gate before charge/PR. `app.py`, `manifest.json`.
- Token leak fixed (GIT_ASKPASS, sanitized errors) + runner command contracts
  match the real kit CLIs. `migration_pr.py`.
- Partner audit endpoint: partner-secret auth + paid-session verify + Connect transfer.
- FIX: `build.py` commerce pages were emitting literal `{API_URL}` / `{{ }}` on
  rebuild — now interpolated; rebuilt docs are placeholder-clean.

### P1 — growth / launch
- Server-routed checkout: every customer CTA goes through on-site server Checkout
  (deadline+kit+utm tagged); **zero** `buy.stripe.com` links on customer pages.
- Funnel analytics: `POST /api/events` + `events` table; beacons from audit/pack/
  drift/success pages + widget; source/utm/kit persisted to Stripe metadata;
  `/status` exposes `funnel_7d`.
- Drift Watch ($19/mo): `/drift/` page + `POST /api/drift/checkout` (subscription).
- Audit→Pack upsell ($299 credit toward $1,499 within 48h) in the delivery email
  and `/success/` page.
- Per-SKU `/success/` onboarding; Drift Watch + Org License exposed on home.
- Workflow clobber guards (concurrency; status-synth key fix; seo-pages full set).
- Stale URLs purged from customer surfaces → `eolkits.com` (repo links stay github.com).

## New endpoints & pages
- `POST /api/drift/checkout` — Drift Watch subscription session (API; under `/api/*`)
- `POST /api/events` — first-party funnel beacon (API; under `/api/*`)
- `GET /pack/setup` — GitHub App post-install callback (API; routed in Caddy)
- `/drift/`, `/success/` — static pages (ship via the `docs/` rsync)

## Go-live checklist — done / remaining

- [x] `.env.production` updated (2 new vars), API redeployed, Caddy `/pack/setup`
      routed, static site rsynced, end-to-end verified. (this session)
- [x] Stripe + GitHub webhooks were already pointing at `eolkits.com` (the prior
      live build processed them with the same secrets) — no dashboard change needed.
- [ ] Optional: confirm the registered GitHub App's **Setup URL** in
      github.com/settings/apps/eolkits-migration-bot is `https://eolkits.com/pack/setup`
      (non-critical — the `installation`/`installation_repositories` webhooks also
      persist the repo→installation mapping).
- [ ] Optional: run a live test Stripe payment to confirm one purchase/job/email.

## Phase 5 — manual-only remainder (cannot be automated here)
- Run the sandbox e2e (`apps/runner/scripts/sandbox_e2e.py`) against a real App
  installation, then paste the real PR URL into `launch/show-hn-final.md` and
  `launch/hn-replies.md` (placeholders are in place).
- Publish the Marketplace tile from the `v1` release.
- Post the Show HN (window refreshed to **Tue Jun 9 / Wed Jun 10, 2026**).
- Delete `HANDOFF.md` (and this file) once 1–5 above are live.

## Deferred (intentional, not blockers)
- `apps/worker/*` is the retired Cloudflare worker, kept as reference per
  `deploy/grace/README.md`; it still contains old `workers.dev` refs (not
  customer-facing). Delete in a follow-up cleanup.

## Validation status
- grace-api: 11 passed · runner: 8 passed · kit suites unchanged.
- Doc gates: no `{API_URL}`/`{{`/`}}`, no stale URLs, sitemap 23/23 `eolkits.com`,
  deterministic double-build. CI job `test-grace-api` + gates added to `.github/workflows/test.yml`.

## Rollback
- Static: untar the webroot snapshot or re-rsync the previous `docs/`.
- API: `docker compose -p eolkits-api` previous image.
- Caddy: restore the prior `eolkits.com` block + reload.
