# Rupture Runner Dependencies
# These are installed in the container for processing jobs

# Core dependencies
boto3>=1.34.0
botocore>=1.34.0
pyyaml>=6.0.1
requests>=2.31.0

# PDF generation
weasyprint>=61.0
jinja2>=3.1.3

# Kit dependencies (installed separately via setup.py)
# - lambda-lifeline
# - al2023-gate
# - python-pivot

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0

# Utilities
tenacity>=8.2.3  # Retry logic
